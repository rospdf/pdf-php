# ignore the vendor directory used by composer
vendor

# ignore error.log being generated by php
error.log

# ignore *.lock files, like composer.lock
*.lock

# ignore the composer.phar if its located in project root
composer.phar

# ignore alle *.pdf file in the examples directory
examples/*.pdf
